name: Quick Test

on:
  workflow_dispatch:

jobs:
  quick-test:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
    - name: Quick test
      run: |
        echo "🚀 Quick test started at $(date)"
        echo "✅ System is working!"
        echo "🏁 Test completed at $(date)"
